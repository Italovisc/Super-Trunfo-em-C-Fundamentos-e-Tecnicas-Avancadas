#include <stdio.h>

int main() {
    // Criação das variáveis
    int num1 = 10, num2, resultado;


    // Atribuição do resultado e output
    resultado = 10;
    printf("O valor inicial é: %d", resultado);


    // Cálculos com Operadores de Atribuição e output
    resultado += 20;
    printf("\nO valor inicial somado é: %d", resultado);

    resultado -= num1;
    printf("\nO valor somado subtraído é: %d", resultado);

    resultado *= 5;
    printf("\nO valor subtraído multiplicado é: %d", resultado);

    resultado /= 2;
    printf("\nO valor multiplicado dividido é: %d", resultado);


    return 0;
}